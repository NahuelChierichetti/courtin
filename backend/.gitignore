node_modules/
.env
.env.*
!.env.example

dist/
build/
# Backups de la base (mongodump). Traen datos reales de clientes —emails,
# teléfonos, movimientos de caja— así que no entran nunca al repo.
courtin-*.gz
*.dump
dump/
